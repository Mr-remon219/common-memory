import type { CoreError } from "../contracts/errors.js";
import type { Revision } from "../contracts/types.js";
export interface RevisionEnvelope { schema_version: 1; knowledge_revision: Revision | null; store_revision: Revision | null; index_revision: Revision | null; warnings: string[] }
export type CoreResponse<T> = ({ ok: true; data: T } & RevisionEnvelope) | ({ ok: false; error: { code: string; message: string; details: Readonly<Record<string, unknown>> } } & RevisionEnvelope);
function revisionWarnings(revisions: { knowledge_revision: Revision; index_revision: Revision | null } | undefined, warnings: readonly string[] = []): string[] { const result = new Set(warnings); if (revisions?.index_revision !== null && revisions?.index_revision !== undefined && revisions.index_revision !== revisions.knowledge_revision) result.add("INDEX_OUTDATED"); return [...result]; }
export function success<T>(revisions: { knowledge_revision: Revision; store_revision: Revision; index_revision: Revision | null }, data: T, warnings: string[] = []): CoreResponse<T> { return { ok: true, schema_version: 1, ...revisions, data, warnings: revisionWarnings(revisions, warnings) }; }
export function failure<T>(error: CoreError, revisions?: { knowledge_revision: Revision; store_revision: Revision; index_revision: Revision | null }): CoreResponse<T> { return { ok: false, schema_version: 1, knowledge_revision: revisions?.knowledge_revision ?? null, store_revision: revisions?.store_revision ?? null, index_revision: revisions?.index_revision ?? null, error: { code: error.code, message: error.message, details: error.details }, warnings: revisionWarnings(revisions) }; }
