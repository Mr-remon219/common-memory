# v0.4.1 本地验证记录

日期：2026-09-13。**本记录对应发布前的工作树验证阶段，当时尚未提交或发布**；以下结果不是 npm registry、GitHub CI 或真实 Desktop UI 验收。后续正式提交及发布结果以 GitHub Release、对应 CI 和 registry 产物为准。

## 被测状态

- 基线：`main@089124ca3ce8ed965784915d72c134fdc35c9ef7`，加当前 v0.4.1 工作树改动（含新增文件）。
- 包版本 / 构建 CLI：`0.4.1`。
- Node：`v22.23.1`；Ubuntu WSL2，内核 `6.18.33.2-microsoft-standard-WSL2`；Windows PowerShell 5.1。
- 源码 / 测试 / 脚本 / manifest 的 SHA-256 清单：`/tmp/common-memory-v041-final-evidence/source.sha256`；清单自身 SHA-256：`83dae4ab8411e9f3b1e0599ddb0b8dbcd2fbf963bf00ed098ca69cc4c7054308`。文档不在此源码清单中。
- 所有存储、凭据与客户端安装使用临时隔离目录、合成输入 / provider；未更改个人记忆、配置或密钥。消费者与 WSL 的 npm 下载保留宿主现有代理环境，不修改代理设置。

## 结果

| 检查 | 结果 |
| --- | --- |
| `npm run release:check` | 通过 |
| `node scripts/verify.mjs` | typecheck、边界检查、61 文件 / 800 测试、build 全部通过 |
| `npm run test:consumer` | 实际 tarball 安装、外部类型导出、Writer 提交 / 重启读取、Pi entry、CLI、无 Key 只读 MCP、受管安装及隔离卸载通过 |
| `npm run test:wsl` | 实际安装包的 WSL PTY TUI、wsl.exe read/init MCP、共享 Work/Codex 安装与移除、原生合成宿主身份 / Unicode / 路径 / refresh / 退出后 Core drain 通过 |
| `npm run test:windows`（WSL interop） | 2 文件 / 9 测试通过；不是 GitHub 原生 Windows CI |
| `npm pack --dry-run --ignore-scripts --json` | 使用已验证构建检查包清单；415 项，无个人 `.env`、SQLite 或 memory 数据；`.env.sample` 是示例 |
| `git diff --check` / 暂存区检查 | 通过；无暂存文件 |

日志归档：`/tmp/common-memory-v041-final-evidence/`。

## 首次失败及修正

- 全量 gate 首次有 4 个旧精确状态断言失败：新 `states` 汇总字段尚未加入 lifecycle / recovery 测试预期。补充 buffered / dead / processed 的精确预期后重新执行完整 gate，800 项通过；未削弱生命周期或持久恢复断言。
- 首次 consumer 在 npm 下载 Pi peer 时超时，隔离环境遗漏宿主代理。保留原日志；只为隔离测试继承现有代理变量后重新执行，通过。未修改依赖、网络实现或全局 dispatcher。
- 独立审查发现并修正：整次 drain 期限、并发成功后过期隔离（包括 rowid 复用）、固定工作区身份、恢复分页、edit maintain 的证据链接。新增定向回归后纳入全量 gate。

## 仍需区分的边界

- 没有真实模型调用，不能据此证明模型记忆判断质量；晚到旧对话覆盖新编辑意图仍是已知限制。
- 不证明真实 Codex / Desktop UI 的信任确认及全部事件组合，也不替代 macOS / Node 24 / 原生 Windows CI。
- 正式发布前仍需停止写端、备份完整 dataRoot，并验证最终提交及对应 CI。本文的本地工作树结果不能冒充最终提交或已发布 registry 产物。
